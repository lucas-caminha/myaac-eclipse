{% set title = t('team.title') %}
{% set content %}
{% set i = 0 %}

<div class="eclipse-directory-page eclipse-team-page">
	{% for group in groupmember|reverse %}
		{% if group.members is not empty %}
			<div class="directory-card team-group-card">
				<div class="directory-card-title team-group-title">
					<span>{{ group.group_name|capitalize }}</span>
					<small>{{ t(group.members|length == 1 ? 'team.member_singular' : 'team.member_plural', {'count': group.members|length}) }}</small>
				</div>
				<table class="directory-table team-table">
					<tbody>
					<tr class="LabelH">
						{% if setting('core.team_outfit') %}
							<td class="team-outfit-cell">{{ t('common.outfit') }}</td>
						{% endif %}
						<td>{{ t('common.name') }}</td>
						{% if setting('core.team_status') %}
							<td>Status</td>
						{% endif %}
						{% if (setting('core.multiworld') or setting('core.team_world')) %}
							<td>{{ t('common.world') }}</td>
						{% endif %}
					</tr>

					{% for member in group.members|reverse %}
						{% set i = i + 1 %}
						<tr>
							{% if setting('core.team_outfit') %}
								<td class="team-outfit-cell">
									<img src="{{ member.outfit }}" alt="outfit">
								</td>
							{% endif %}
							<td>
								{% if setting('core.account_country') %}
									{{ member.flag_image|raw }}
								{% endif %}
								{{ member.link|raw }}
							</td>
							{% if setting('core.team_status') %}
								<td>
									{% if member.status %}
										<span class="status-online">Online</span>
									{% else %}
										<span class="status-offline">Offline</span>
									{% endif %}
								</td>
							{% endif %}
							{% if (setting('core.multiworld') or setting('core.team_world')) %}
								<td><strong>{{ member.world_name }}</strong></td>
							{% endif %}
						</tr>
					{% endfor %}
					</tbody>
				</table>
			</div>
		{% endif %}
	{% else %}
		<div class="directory-card">
			<div class="directory-empty">{{ t('team.empty') }}</div>
		</div>
	{% endfor %}
</div>

{% endset %}
{% include 'tables.headline.html.twig' %}
