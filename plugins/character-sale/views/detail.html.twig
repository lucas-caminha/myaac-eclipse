<div class="eclipse-plugin-page eclipse-character-market eclipse-market-detail-page">
{% if offer %}
	<header class="eclipse-plugin-hero eclipse-market-detail-hero">
		<div class="eclipse-market-detail-outfit"><img src="{{ offer.outfit }}" alt="{{ offer.name }}"></div>
		<div>
			<span class="eclipse-plugin-kicker">Personagem à venda</span>
			<h2>{{ offer.name }}</h2>
			<p>Nível {{ offer.level }} • {{ offer.vocation_name }} • {{ offer.status_name }}</p>
			<strong class="eclipse-market-price">{{ offer.price|number_format(0, ',', '.') }} coins</strong>
		</div>
	</header>

	<div class="eclipse-market-detail-actions">
		<a class="eclipse-plugin-button eclipse-plugin-button-secondary" href="{{ getLink('character-sale') }}">Voltar ao mercado</a>
		{% if logged %}
			<form action="{{ getLink('character-sale') }}" method="post">
				{{ csrf() }}
				<input type="hidden" name="offer_id" value="{{ offer.id }}">
				{% if offer.seller_account_id == accountId %}
					<input type="hidden" name="market_action" value="cancel">
					<button class="eclipse-plugin-button eclipse-plugin-button-danger" type="submit">Cancelar oferta</button>
				{% else %}
					<input type="hidden" name="market_action" value="buy">
					<button class="eclipse-plugin-button" type="submit" onclick="return confirm('Confirmar a compra de {{ offer.name|e('js') }}?')">Comprar personagem</button>
				{% endif %}
			</form>
		{% else %}
			<a class="eclipse-plugin-button" href="{{ getLink('account/manage') }}">Entrar para comprar</a>
		{% endif %}
	</div>

	<section class="eclipse-plugin-section">
		<h3>Resumo</h3>
		<div class="eclipse-market-detail-stats">
			{% for stat in summaryStats %}
				<div><span>{{ stat.label }}</span><strong>{{ stat.value }}</strong></div>
			{% endfor %}
		</div>
	</section>

	<section class="eclipse-plugin-section">
		<h3>Informações</h3>
		<div class="eclipse-market-info-list">
			{% for stat in infoStats %}
				<div><span>{{ stat.label }}</span><strong>{{ stat.value }}</strong></div>
			{% endfor %}
		</div>
	</section>

	<section class="eclipse-plugin-section">
		<h3>Progressão</h3>
		<div class="eclipse-market-detail-stats">
			{% for stat in progressStats %}
				<div><span>{{ stat.label }}</span><strong>{{ stat.value }}</strong></div>
			{% endfor %}
		</div>
	</section>

	{% if combatStats is not empty %}
	<section class="eclipse-plugin-section">
		<h3>Combate</h3>
		<div class="eclipse-market-detail-stats">
			{% for stat in combatStats %}
				<div><span>{{ stat.label }}</span><strong>{{ stat.value }}</strong></div>
			{% endfor %}
		</div>
	</section>
	{% endif %}

	<section class="eclipse-plugin-section">
		<h3>Skills</h3>
		<div class="eclipse-market-skill-grid">
			{% for skill in skills %}
				<div><span>{{ skill.label }}</span><strong>{{ skill.value }}</strong></div>
			{% endfor %}
		</div>
	</section>

	<section class="eclipse-plugin-section">
		<h3>Equipamentos</h3>
		{% if equipment is not empty %}
			<div class="eclipse-market-equipment-grid">
				{% for item in equipment %}
					<div><img src="{{ item.image }}" alt="{{ item.name }}"><span>{{ item.slot }}</span><strong>{{ item.name }}</strong>{% if item.count > 1 %}<small>x{{ item.count }}</small>{% endif %}</div>
				{% endfor %}
			</div>
		{% else %}
			<p class="eclipse-plugin-note">Nenhum equipamento visível encontrado.</p>
		{% endif %}
	</section>

	{% if deaths is not empty %}
	<section class="eclipse-plugin-section">
		<h3>Mortes recentes</h3>
		<div class="eclipse-market-death-list">
			{% for death in deaths %}
				<div><span>{{ death.date }} • nível {{ death.level }}</span><strong>{{ death.killed_by }}</strong>{% if death.mostdamage_by %}<small>Maior dano: {{ death.mostdamage_by }}</small>{% endif %}</div>
			{% endfor %}
		</div>
	</section>
	{% endif %}

	{% if offer.comment %}
	<section class="eclipse-plugin-section">
		<h3>Comentário público</h3>
		<p class="eclipse-market-comment">{{ offer.comment }}</p>
	</section>
	{% endif %}
{% else %}
	<div class="eclipse-plugin-empty">Oferta não encontrada.</div>
	<a class="eclipse-plugin-button eclipse-plugin-button-secondary" href="{{ getLink('character-sale') }}">Voltar ao mercado</a>
{% endif %}
</div>
